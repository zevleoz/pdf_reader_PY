# Tasks

- [x] Task 1: 学生重命名 API + 学生档案 UI 改版
  - [x] 1.1 在 `app.py` 新增 `PUT /api/students/<int:student_id>` 路由，接受 JSON `{"name": "..."}`，更新 `students.name`，返回 `{ok: true, student: {...}}`；不触碰其他 Python 逻辑
  - [x] 1.2 改版 `templates/students.html` 列表区：从基础表格改为精炼卡片/行布局，突出姓名+年级+Y4 状态，操作按钮分组（查看报告、新建议程、Rename、删除），减少杂乱
  - [x] 1.3 在学生行/卡片上添加 Rename 按钮，点击弹出内联编辑（当前姓名 → 输入新姓名 → 确认调用 `PUT /api/students/<id>` → 刷新列表），含取消/确认
  - [x] 1.4 保持现有"查看报告"展开逻辑、报告列表、纪要历史、CSV 导出、刷新等功能不变；适配新布局视觉

- [x] Task 2: 解读会纪要学生选择器
  - [x] 2.1 在 `templates/transcript.html` 表单区添加学生下拉选择器（`<select>`），页面加载时从 `/api/students` 拉取列表填充
  - [x] 2.2 选中学生后自动填充姓名、年级、性别字段（保留字段可编辑）
  - [x] 2.3 支持 URL 参数 `?student_id=X` 自动选中对应学生
  - [x] 2.4 从学生档案页"新建议程"跳转时自动关联（现有链接逻辑不变，只改接收端）
  - [x] 2.5 保持逐字稿输入、纪要生成/编辑/下载/删除等流程完全不变

- [x] Task 3: 内部下载 UI 打磨
  - [x] 3.1 打磨 `templates/internal.html` 布局：清晰化步骤引导（选学生 → 选报告 → 运行解读 / 查看数据 → 下载），加 section 标题
  - [x] 3.2 优化空状态提示文案，让首次用户知道该做什么
  - [x] 3.3 统一按钮/选择器/状态指示器的视觉风格（与 style.css 一致），去掉粗糙感
  - [x] 3.4 保持所有工具功能不变：AI 解读、有效评价、E4 映射、Y4/E4 切换、下载等

- [x] Task 4: 验证
  - [x] 4.1 启动服务，登录后访问三个页面确认无控制台错误
  - [x] 4.2 学生档案：Rename 功能可改名且持久化；报告列表/纪要历史正常展开
  - [x] 4.3 解读会纪要：学生选择器加载正常；选中后自动填充；URL 参数自动选中
  - [x] 4.4 内部下载：选学生→选报告→运行解读流程正常；E4 映射/Y4/E4 切换正常
  - [x] 4.5 确认现有页面 JS 逻辑（upload slots、calendar、prompt lab）不受影响

# Task Dependencies
- Task 1, 2, 3 可并行（不同文件）
- Task 4 依赖 1-3 完成
